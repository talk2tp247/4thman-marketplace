{
  "name": "4thman-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "Global marketplace for buying and selling products",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "db:push": "cd apps/marketplace && npx prisma db push",
    "db:seed": "cd apps/marketplace && npx prisma db seed",
    "db:studio": "cd apps/marketplace && npx prisma studio"
  },
  "devDependencies": {
    "turbo": "^1.11.0",
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
